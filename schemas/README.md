# Schemas

P11A skeleton stores schema placeholders here. The current concrete host schemas live in:

```text
remide/docs/process/captain-os-lab/schemas/
```

They should be copied into this repo during the package implementation slice.
